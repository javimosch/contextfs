---
phase: 10-core-command-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/mcp/tools/token-tracker.js
  - server/mcp/tools/token-logger.js
  - server/mcp/tools/command-parser.js
  - test/token-tracker.test.js
  - test/command-parser.test.js
autonomous: true
requirements:
  - CORE-06
must_haves:
  truths:
    - Token savings are calculated for every RTK command execution
    - Token reduction percentage is logged to console
    - Per-command statistics are accumulated
    - Periodic reports show aggregate token savings
    - Token logs are persisted to workspace/.rtk/logs/
  artifacts:
    - path: server/mcp/tools/token-tracker.js
      provides: TokenTracker class with estimateTokens(), record(), getSummary()
      exports: [TokenTracker]
    - path: server/mcp/tools/token-logger.js
      provides: TokenLogger class for persistent JSONL logging
      exports: [TokenLogger]
    - path: server/mcp/tools/command-parser.js
      provides: CommandParser for native: prefix detection
      exports: [CommandParser]
  key_links:
    - from: TokenTracker.record()
      to: console.log with savings message
      via: "console.log(`[RTK] ${command} saved ${savings} chars...`)"
    - from: TokenLogger.logExecution()
      to: /workspace/.rtk/logs/tokens-YYYY-MM-DD.jsonl
      via: fs.appendFile
---

<objective>
Create infrastructure for tracking and logging token reduction metrics from RTK command execution.

Purpose: Enable measurement and verification of 60-80% token reduction targets by tracking character savings per command and accumulating statistics.
Output: TokenTracker, TokenLogger, and CommandParser modules with comprehensive test coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mcp-integration-layer/09-04-SUMMARY.md
@server/mcp/tools/rtk-executor.js

## Interfaces from RTKExecutor (Phase 9)

RTKExecutor.execute() returns:
```javascript
{
  stdout: string,
  stderr: string,
  exitCode: number,
  source: 'rtk' | 'native'
}
```

RTKExecutor.isSupportedCommand(command, args) returns boolean
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TokenTracker module</name>
  <files>server/mcp/tools/token-tracker.js, test/token-tracker.test.js</files>
  <action>
Create server/mcp/tools/token-tracker.js with TokenTracker class that:
- estimateTokens(text): Returns estimated token count using ~4 chars/token heuristic
- record(command, source, output, nativeChars): Records execution with savings calculation
- getSummary(): Returns aggregated statistics including total commands, RTK usage %, avg reduction %
- reportIfNeeded(): Logs periodic reports every 100 commands

Statistics to track:
- totalCommands, rtkCommands, nativeCommands
- totalNativeChars, totalRTKChars
- savingsByCommand Map with per-command counts

Log format: "[RTK] {command}: saved {savings} chars ({pct}% reduction)"

Create test/token-tracker.test.js with tests for:
- estimateTokens accuracy
- record() with RTK source calculates savings
- record() with native source no savings
- getSummary() returns correct aggregates
- Per-command statistics accumulation
</action>
  <verify>
    <automated>node --test test/token-tracker.test.js</automated>
  </verify>
  <done>
TokenTracker passes all tests, exports estimateTokens, record, getSummary, reportIfNeeded methods. Console logging shows savings messages in correct format.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TokenLogger module</name>
  <files>server/mcp/tools/token-logger.js, test/token-logger.test.js</files>
  <action>
Create server/mcp/tools/token-logger.js with TokenLogger class that:
- Constructor accepts logDir (default: '/workspace/.rtk/logs')
- logExecution({command, args, source, nativeChars, rtkChars, exitCode, timestamp}): 
  - Calculates savings and reduction percentage
  - Creates JSONL entry with timestamp, session, command, source, metrics, exit_code
  - Writes to daily log file: tokens-YYYY-MM-DD.jsonl
  - Also logs to console for RTK commands

Metrics object:
```javascript
{
  native_chars: number,
  rtk_chars: number,
  char_savings: number,
  reduction_percent: number,
  estimated_tokens_saved: number
}
```

Create test/token-logger.test.js with tests for:
- Log file creation in correct directory
- JSONL format (one JSON object per line)
- Correct metrics calculation
- Daily log file rotation based on timestamp
</action>
  <verify>
    <automated>node --test test/token-logger.test.js</automated>
  </verify>
  <done>
TokenLogger creates /workspace/.rtk/logs/ directory, writes valid JSONL entries with correct metrics, rotates files daily.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CommandParser module</name>
  <files>server/mcp/tools/command-parser.js, test/command-parser.test.js</files>
  <action>
Create server/mcp/tools/command-parser.js with CommandParser class that:
- Static parse(line): Parses command line and detects native: prefix
- Returns {bypass: boolean, command: string, args: string[]}
- Handles formats: "native:ls -la" and "ls native:ls -la" (embedded in first arg)
- Strips native: prefix when bypass is true
- formatForLog(command, args): Formats command for logging

Edge cases to handle:
- Empty/invalid input returns empty command
- "native:" alone should return empty
- Arguments containing "native:" substring (not prefix) should not trigger bypass
- Whitespace normalization

Create test/command-parser.test.js with tests for:
- Normal command parsing (no prefix)
- native: prefix detection and stripping
- Embedded native: in first arg
- Edge cases (empty, whitespace, substring match)
</action>
  <verify>
    <automated>node --test test/command-parser.test.js</automated>
  </verify>
  <done>
CommandParser correctly identifies native: prefix in all formats, strips prefix when bypassing, handles edge cases. All tests pass.
  </done>
</task>

</tasks>

<verification>
Wave 1 complete when:
1. node --test test/token-tracker.test.js passes (≥ 6 tests)
2. node --test test/token-logger.test.js passes (≥ 4 tests)
3. node --test test/command-parser.test.js passes (≥ 6 tests)
4. All modules export correct interfaces
5. No linting errors
</verification>

<success_criteria>
- TokenTracker records RTK executions with character savings calculations
- TokenLogger persists metrics to JSONL files in /workspace/.rtk/logs/
- CommandParser correctly detects and handles native: prefix bypass
- All tests pass with >80% code coverage
- Modules can be imported by downstream Wave 2 components
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-command-integration/10-01-SUMMARY.md`
</output>
