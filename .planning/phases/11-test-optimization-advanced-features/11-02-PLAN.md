---
phase: 11-test-optimization-advanced-features
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - server/mcp/tools/rtk-executor.js
  - server/mcp/mcp-tools.js
  - server/mcp/mcp-server.js
  - test/rtk-read.test.js
  - test/rtk-compact.test.js
  - test/mcp-tools-registry.test.js
requirements: [ADV-01, ADV-02, ADV-03, ADV-04]
autonomous: true
must_haves:
  truths:
    - "contextfs.read filters large files (>500 lines) showing start, end, and middle summary"
    - "contextfs.smart provides signature and purpose summary of code files"
    - "Ultra-compact mode (-u) aggressively strips whitespace and formatting"
    - "git log and ls have specialized compact formats when ultra-compact is active"
  artifacts:
    - path: "server/mcp/mcp-tools.js"
      provides: "Definition for contextfs.smart and updated contextfs.read"
    - path: "server/mcp/tools/rtk-executor.js"
      provides: "Implementation of read filtering, smart summary logic, and ultra-compact mode"
  key_links:
    - from: "server/mcp/mcp-server.js"
      to: "server/mcp/tools/rtk-executor.js"
      via: "tool handler calls"
---

<objective>
Implement specialized summarization tools (read, smart) and a global ultra-compact mode for maximum token efficiency.

Purpose: Provide high-utility, low-cost alternatives for codebase exploration and reading large files.
Output: New smart tool, enhanced read tool, and ultra-compact formatting across core commands.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@server/mcp/tools/rtk-executor.js
@server/mcp/mcp-tools.js
@server/mcp/mcp-server.js
@.planning/phases/11-test-optimization-advanced-features/11-01-SUMMARY.md

<interfaces>
From server/mcp/tools/rtk-executor.js (after 11-01):
```javascript
class RTKExecutor {
  execute(command, args = [], options = {})
  mapToRTKArgs(command, args)
  isSupportedCommand(command, args)
}
```

From server/mcp/mcp-tools.js:
```javascript
const TOOLS = [ ... ];
module.exports = { getAllTools, getToolDefinition, TOOLS };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement read and smart support in RTKExecutor</name>
  <files>server/mcp/tools/rtk-executor.js, test/rtk-read.test.js</files>
  <action>
    1. Add 'read' to ALLOWLIST and mapToRTKArgs to support 'rtk read' subcommand.
    2. Implement large file filtering logic in executeRTK (or post-processing helper):
       - For files > 500 lines: show first 100 lines, last 100 lines, and a summary of middle lines (e.g., "Filtered X lines...").
       - Preserve line numbers in the output.
    3. Implement 'smart' summary logic:
       - Use 'rtk read --level minimal' to get signatures, docstrings, and exports.
       - Calculate a basic complexity rating (Low: <100 lines, Med: 100-500, High: >500).
       - Include line count metadata.
       - Short-circuit: Return full content for files < 10 lines.
    4. Ensure ADV-03 (Tee output recovery) is functional for these tools by leveraging existing saveTeeOutput.
    5. Create test/rtk-read.test.js (Wave 0 gap) to verify filtering and smart summaries.
  </action>
  <verify>
    <automated>node test/rtk-read.test.js</automated>
  </verify>
  <done>RTKExecutor supports intelligent file reading and code summarization.</done>
</task>

<task type="auto">
  <name>Task 2: Register and wire contextfs.smart and contextfs.read</name>
  <files>server/mcp/mcp-tools.js, server/mcp/mcp-server.js, test/mcp-tools-registry.test.js</files>
  <action>
    1. Add 'contextfs.smart' to TOOLS array in server/mcp/mcp-tools.js.
    2. Update 'contextfs.read' in TOOLS array to include an optional 'largeFileFilter' boolean property.
    3. Update server/mcp/mcp-server.js to:
       - Register a handler for 'contextfs.smart' that calls RTKExecutor.
       - Update 'contextfs.read' handler to pass filtering options to RTKExecutor.
    4. Create test/mcp-tools-registry.test.js to verify tools are registered and handlers are wired correctly.
  </action>
  <verify>
    <automated>node test/mcp-tools-registry.test.js</automated>
  </verify>
  <done>The new and updated tools are available via the MCP server.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Ultra-Compact Mode</name>
  <files>server/mcp/tools/rtk-executor.js, test/rtk-compact.test.js</files>
  <action>
    1. Add support for '-u' flag in RTKExecutor, passing it to RTK subcommands when present in options.
    2. Support CONTEXTFS_RTK_ULTRA_COMPACT=true environment variable to enable this mode globally in RTKExecutor.
    3. Implement specialized JS-side post-formatting for specific commands when ultra-compact is active:
       - git log: regex replace to "[short-hash] [first-30-chars-of-msg]".
       - ls: regex replace to "[filename] [size]".
    4. Aggressively strip whitespace and redundant labels from RTK output when in this mode.
    5. Create test/rtk-compact.test.js (Wave 0 gap) to verify token reduction and formatting.
  </action>
  <verify>
    <automated>node test/rtk-compact.test.js</automated>
  </verify>
  <done>Ultra-compact mode provides maximum token compression for core operations.</done>
</task>

</tasks>

<verification>
Verify contextfs.read handles large files correctly.
Verify contextfs.smart provides accurate summaries and complexity ratings.
Verify ultra-compact mode works both via flag and environment variable.
Check that line numbers are preserved in filtered outputs.
</verification>

<success_criteria>
- contextfs.smart tool operational and returns summaries.
- contextfs.read uses RTK filtering for files > 500 lines.
- Ultra-compact mode achieves higher compression than standard RTK output.
- Requirements ADV-01, ADV-02, ADV-03, ADV-04 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-optimization-advanced-features/11-02-SUMMARY.md`
</output>
