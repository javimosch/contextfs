---
phase: 11-test-optimization-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/mcp/tools/bash-rtk-adapter.js
  - server/mcp/tools/rtk-executor.js
  - test/bash-rtk-adapter.test.js
  - test/rtk-test-output.test.js
  - test/rtk-token-savings.test.js
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, ADV-05]
autonomous: true
must_haves:
  truths:
    - "npm test is routed through RTK when no complex shell characters are present"
    - "cargo test is routed through RTK when no complex shell characters are present"
    - "Test output is limited to first 5 failures with full stack traces"
    - "Success summary is appended to test output"
  artifacts:
    - path: "server/mcp/tools/bash-rtk-adapter.js"
      provides: "Test command pattern detection"
    - path: "server/mcp/tools/rtk-executor.js"
      provides: "Test output post-processing and failure limiting"
  key_links:
    - from: "server/mcp/tools/bash-rtk-adapter.js"
      to: "server/mcp/tools/rtk-executor.js"
      via: "execute call for test commands"
---

<objective>
Enable RTK proxying for test commands (npm test, cargo test, etc.) with intelligent failure filtering and token reduction.

Purpose: Reduce token consumption for high-verbosity test suites while maintaining debugging utility.
Output: Updated adapter and executor supporting test-specific optimization.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@server/mcp/tools/bash-rtk-adapter.js
@server/mcp/tools/rtk-executor.js
@test/bash-rtk-adapter.test.js

<interfaces>
From server/mcp/tools/bash-rtk-adapter.js:
```javascript
class BashRTKAdapter {
  constructor(dependencies = {})
  parseScript(script)
  shouldUseRTKForLine(line, parsed)
  executeScript(script, options = {})
}
```

From server/mcp/tools/rtk-executor.js:
```javascript
class RTKExecutor {
  constructor(config = {})
  execute(command, args = [], options = {})
  isSupportedCommand(command, args)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BashRTKAdapter for test pattern detection</name>
  <files>server/mcp/tools/bash-rtk-adapter.js, test/bash-rtk-adapter.test.js</files>
  <action>
    Add test-related commands ('npm', 'cargo', 'pytest', 'vitest', 'jest') to rtkCommands.
    Update shouldUseRTKForLine to also match test patterns like *test.js or *.spec.ts by checking if the command tokens contain these patterns.
    Update tests in test/bash-rtk-adapter.test.js to verify routing of test commands.
  </action>
  <verify>
    <automated>node test/bash-rtk-adapter.test.js</automated>
  </verify>
  <done>Test commands are correctly identified as simple commands for RTK routing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Test-Specific Logic in RTKExecutor</name>
  <files>server/mcp/tools/rtk-executor.js, test/rtk-test-output.test.js</files>
  <action>
    1. Update ALLOWLIST to include 'npm' (with 'test' flag), 'cargo' (with 'test' flag), 'pytest', 'vitest', 'jest'.
    2. Update mapToRTKArgs to map these commands to their respective RTK subcommands (e.g., 'npm test' -> ['npm', 'test']).
    3. Implement output post-processing for test commands in executeRTK or a helper:
       - Limit failures to first 5, include full stack traces.
       - Append "X passed, Y failed" summary based on RTK output format.
       - For timeouts, return last 50 lines recorded.
    4. Support ADV-05 by ensuring mapToRTKArgs preserves supported flags for these new commands.
    5. Create test/rtk-test-output.test.js to verify failure limiting and summary formatting.
  </action>
  <verify>
    <automated>node test/rtk-test-output.test.js</automated>
  </verify>
  <done>RTKExecutor handles test output according to user decisions, limiting failures and providing summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Token Reduction for Tests</name>
  <files>server/mcp/tools/rtk-executor.js, test/rtk-token-savings.test.js</files>
  <action>
    1. Create test/rtk-token-savings.test.js that runs a representative test suite through the updated RTK executor.
    2. Measure the token reduction using the existing token tracking infrastructure (token-tracker.js/token-logger.js).
    3. Verify that TEST-05 (85-90% reduction) is achieved.
  </action>
  <verify>
    <automated>node test/rtk-token-savings.test.js</automated>
  </verify>
  <done>Test token reduction is verified to be within the 85-90% target range.</done>
</task>

</tasks>

<verification>
Ensure all test runners (npm, cargo, pytest) are correctly routed and optimized.
Check that failure limiting works for suites with >5 failures.
Verify that success summary is always present.
</verification>

<success_criteria>
- npm test/cargo test routed through RTK.
- Test output correctly truncated to 5 failures + summary.
- Measured token reduction for tests > 85%.
</success_criteria>

<output>
After completion, create `.planning/phases/11-test-optimization-advanced-features/11-01-SUMMARY.md`
</output>
