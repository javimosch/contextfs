1. Phase scope — Which phases this session? (first iteration)

 • A — Phases 1–3: CLI + new client + server with scheduler/registry + dashboard UI (no MCP)
 • B — Phases 1–4: Everything above + MCP server (stdio + SSE) with all 9 tools ← most useful stopping point
 • C — All 7 phases: includes chat TUI, Docker strategy, docs

client+server (ws mode) + jest tests
(no docker, no --local, no UI, no MCP)

2. Persistence — What should the server use to store registry state (clients, virtual clients, workspaces, API keys)
across restarts?

 • A — JSON flat files under ~/.contextfs/ (zero deps, survives restarts) ← my recommendation
 • B — In-memory only (lost on restart, simplest for v1)
 • C — SQLite via better-sqlite3

JSON (will upgrade to mongodb layer, add db wrapper layer)

3. HTTP server — For the dashboard + WS server:

 • A — Express.js (already in monorepo)
 • B — Node built-in http (zero new dep)

expressjs (install deps again in cwd (contextfs folder), do not depend on monorepo

4. MCP SDK (if Phase 4):

 • A — @modelcontextprotocol/sdk (official Anthropic SDK)
 • B — Raw stdio/SSE implementation (no library)

raw stdio/sse implementation (no typescript)

5. Client — For npx contextfs client:

 • A — Build new contextfs/client/ module with CPU/RAM capability reporting for the scheduler
 • B — Wire client-legacy into the CLI now, new client later

A => new client (no opencode)

6. Default port — The initial spec shows ws://localhost:3010. Should the server default to 3010?

OK, but configurable via PORT

7. bash_script_once in --local mode — Allowed by default, or behind a --allow-bash flag?

behind --insecure

8. API keys — Auto-generated by server on WS-client/virtual-client creation (shown in dashboard), or set manually?

yes (user can regen)
